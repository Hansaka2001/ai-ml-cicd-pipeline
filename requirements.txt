# Core API framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Machine Learning
scikit-learn>=1.4.0
joblib>=1.4.0

# Data handling
datasets>=2.19.0
numpy>=1.26.0
pandas>=2.2.0

# Data validation (Pydantic v2 — bundled with FastAPI but explicit is better)
pydantic>=2.7.0

# Testing
pytest>=8.2.0
httpx>=0.27.0        # sync/async HTTP client used by TestClient

# Code quality
black>=24.4.0        # code formatter
ruff>=0.4.0          # linter (replaces flake8 + isort + pyupgrade)